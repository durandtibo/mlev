r"""DataFrame-oriented object metric APIs.

Use this namespace for metric objects that consume Polars DataFrame
inputs.

Example:
    Import this namespace to access DataFrame-oriented metric objects:

    ```pycon
    >>> import mlev.metrics.frame as metrics_frame
    >>> metrics_frame.__name__
    'mlev.metrics.frame'

    ```
"""
