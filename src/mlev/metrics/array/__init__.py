r"""Array-oriented object metric APIs.

Use this namespace for metric objects that consume array-like prediction
and target inputs.

Example:
    Import this namespace to access array-oriented metric objects:

    ```pycon
    >>> import mlev.metrics.array as metrics_array
    >>> metrics_array.__name__
    'mlev.metrics.array'

    ```
"""
